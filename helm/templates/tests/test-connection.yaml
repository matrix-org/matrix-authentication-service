apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mas.fullname" . }}-test-connection"
  labels:
    {{- include "mas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
